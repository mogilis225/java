server:
  port: 7001
  
spring:
  application:
    name: EmployeeMS
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML 